1. **Linear Transformation**:
   $`\sqrt{3x-1}+(1+x)^2`$

2. **Activation Function** (ReLU):
   - \( a^{(1)} = \text{ReLU}(z^{(1)}) \)

### Layer 2 (fc2):
1. **Linear Transformation**:
   - \( z^{(2)} = a^{(1)} \cdot W_2 + b_2 \)

2. **Output**:
   - \( a^{(2)} = z^{(2)} \)
